{
  "name": "orchestrator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp -r node_modules dist/",
    "test": "jest",
    "deploy": "zip -r function.zip dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.879.0",
    "@aws-sdk/client-dynamodb": "^3.879.0",
    "@aws-sdk/client-opensearch": "^3.879.0",
    "@aws-sdk/client-opensearchserverless": "^3.879.0",
    "@aws-sdk/client-s3": "^3.879.0",
    "aws-lambda": "^1.0.7",
    "axios": "^1.11.0",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
